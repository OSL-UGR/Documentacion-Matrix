
En este documento se detallan los casos de uso principales, los comandos más relevantes para interactuar con el bot y las estrategias de moderación que se han implementado.

Draupnir se controla exclusivamente a través de comandos enviados por chat en la sala que hemos configurado previamente. Para ver el listado completo de opciones siempre podemos enviar el comando `!draupnir help`

> **NOTA IMPORTANTE:** es clave indicar que el bot SOLO podrá gestionar y vigilar salas que no se encuentren bajo cifrado Extremo a Extremo. Por esta razón solo podremos configurarlo para salas públicas.

1. **Comandos principales para la interacción:**

Draupnir divide su funcionamiento en la gestión de salas protegidas, baneos y acciones a nivel de servidor:

**a) Gestión de salas:**

> Para que el bot proteja una sala, primero hay que inivtarlo a la misma y darle permisos de Administrador. Después desde `draupnir-sala` le indicamos que la vigile:

- Añadir una sala para que lo vigile: `!draupnir rooms add <id_sala>`
- Ver listado de salas vigiladas: `!draupnir rooms`

**b) Gestión de baneos:**

> Draupnir no se encarga de ir baneando al aire, sino que agrupa a los que el mismo detecte como infractores en listas previamente creadas por nosotros (Por ejemplo lista de Spam)

- Crear una lista de baneos: `!draupnir list create <nombre-corto> <Nombre Largo>` (Ej: `!draupnir list create spam Lista-Spam-UGR`).

- Banear a un usuario: `!draupnir ban <@usuario:matrix.ugr.es> <nombre-lista> <motivo>`

- Desbanear: `!draupnir unban <@usuario:matrix.ugr.es>`

- Borrar mensajes masivos: `!draupnir redact <@usuario:matrix.ugr.es>`. Este comando elimina de golpe todos los mensajes recientes de un usuario en las salas en las que se encuentre draupnir. 

**c) Acciones de Servidor (administrador):**

Gracias a los permisos delegados en la configuración, el bot puede actuar directamente sobre la base de datos de Synapse. Esto está implementado de esta manera por si queremos añadir algún moderador dentro de matrix, haciendo que tenga permiso al chat con este bot, pero al mismo tiempo no queramos darles permisos de administrador como tal en la base de datos.

- Congelar una : `!draupnir suspend <@usuario:matrix.ugr.es>`. El usuario podrá entrar a Matrix, pero no interactuar.

- Destruir una cuenta : `!draupnir deactivate <@usuario:matrix.ugr.es>`. Elimina la cuenta por completo.

- Tomar el control de una sala: `!draupnir hijack room <ID_DE_LA_SALA> <@tu_usuario:matrix.ugr.es>`. Te otorga permisos de administrador en cualquier sala si el administrador original pierde la cuenta.

2. **Protecciones de Draupnir**

Draupnir incorpora lo que se conocen como "Protecciones", que son módulos que el va ejecutando de forma automática sin necesidad de que escribamos comandos. Por defecto ya trae algunos módulos encendidos, pero se ha decidido encender algunos más para mas seguridad y configuración.

> Para ver todas las protecciones usamos: `!draupnir protections` 
> Para activar una en concreto: `!draupnir protections enable <NombreDeLaProteccion>`


**Protecciones que se han activado que no lo estaban por defecto:**

- `JoinWaveShortCircuitProtection`: SI detecta que una oleada de usuarios se une a una sala en muy pocos segundos, cierra la entrada poniendo esta en modo de "solo por invitación". Esto está activado para evitar ataques masivos de bots.

- `MentionLimitProtection`: Borra auromáticamente mensajes de usuarios que menciones a decenas de personas a la vez por los canales públicos para evitar el spam de notificaciones.

- `HomeserverUserPolicyProtection`: Esta protección se encarga de congelar la cuenta en todo el servidor Synapse de un usuario que ha sido baneado a mano de nuestro propio de servidor, para que no vaya a molestar a otras salas.

- `WordListProtection`: Es el filtro automático que banea a los usuarios que utilicen palabras de una lista negra justo **después de unirse a la sala**, lo que resulta ideas para los bots que envían insultos o enlaces con fraudas de phishing.

> **NOTA IMPORTANTE:** No se aconseja aunque suene atractivo activar la función `BasicFloodingProtection`. La documentación nos indica que contiene errores heredados. Además tampoco se recomienda ser ultra-restrictivo para evitar "falsos positivos".

3. **Insertar el listado de Palabras Malsonantes:**

Para añadir, quitar palabras y mostras las pralabras actuales dentro del `WordListProtection` podemos utilizar los siguientes comandos:

``` chat
# Para añadir palabras
!draupnir protections config add WordList words "palabra"

# Para mostrar el listado actual de palabras
!draupnir protections show WordList

# Para eliminar palabras
!draupnir protections config remove WordList words "palabra"
```

Como añadir un listado grande uno a uno por mensaje es muy ineficiente, lo que hemos echo ha sido configurar un script de Python que lee un archivo de texto con nuestro listado de palabras a mano y se las envía automáticamente a Draupnir a través de la API del servidor.